
#include "libft.h"

void	*ft_memchr(const void *s, int c, size_t n)
{
	unsigned char	chr;
	unsigned char	*res;

	chr = (unsigned char)ch;
	res = (unsigned char *)ptr;
	while (count--)
	{
		if (*res == ch)
			return (res);
		res++;
	}
	return (NULL);
}
